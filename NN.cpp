#include <iostream>
#include <math.h>
#include <vector>
using namespace std;

template<typename T> T sigmoid(T x) {
    return 1 / (1 + exp(-x));
}

class N {
public:
    //activation function

private:
};

class NN {
public:
private:
};


int main() {

    return 0;
}