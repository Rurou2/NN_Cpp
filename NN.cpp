#include <iostream>
#include <math.h>
using namespace std;

double sigmoid(double x) {
    return 1 / (1 + exp(-x));
}

double ReLU(double x) {
    return (x > 0) ? x : 0; 
}

class N {
public:
    //activation function

private:
};

class NN {
public:
private:
};


int main() {
    return 0;
}